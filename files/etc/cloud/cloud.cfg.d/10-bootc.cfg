# In image mode, the host root filesystem is mounted at /sysroot, not /.
# That is the one growpart should resize, not whatever is mounted at /.
# See https://gitlab.com/fedora/bootc/examples/-/tree/main/cloud-init.
growpart:
  mode: auto
  devices: ["/sysroot"]

resize_rootfs: false
