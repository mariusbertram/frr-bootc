site:
  name: dc1
  node_selector_value: dc1
  # Smaller staging cluster: single replica is enough here, well below
  # cluster-forge-a's redundancy - "n replicas per site AND per cluster" is
  # this one number, not hard-coded anywhere shared.
  replicas: 1
  mac_base: "02:00:00:b1:01"
  networks:
    lan:
      ip: 10.30.1.1
      prefix_length: 24
      next_hop_address: 10.30.1.254
